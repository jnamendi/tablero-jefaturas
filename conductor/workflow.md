# Workflow

Standard git branch development flow and local validation using standard tests. Since this workspace starts fresh, we initialize the project structure, split the monolithic codebase, and ensure modularity.
